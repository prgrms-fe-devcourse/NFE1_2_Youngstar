// import useFetchUsers from "../hooks/useFetchUsers";

const MyPage = () => {
    // const { data } = useFetchUsers();

    return (
        <>
            <div>Mypage</div>
            {/* {data && (<div>{data[0]}</div>)} */}
        </>
    );
}

export default MyPage;
